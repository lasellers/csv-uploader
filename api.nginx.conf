worker_processes 4;

events { worker_connections 1024; }

http {
    include mime.types;
    server {
        listen 80;
        listen [::] 80;
        root /var/www/html/public;
        index index.html;
        server_name localhost;
    }
}